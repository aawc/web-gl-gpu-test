# Standing Instructions

Create a static website to be hosted on GitHub Pages that shows the smallest PoCs for the use of WebGL and WebGPU—the user gets to choose which one they want.

## Requirements
- All artifacts should be placed in the current directory.
- Generate `PROMPT.md` and `README.md` with each change.
- **Do not** add tags such as `AGY` and `CONV` or any other tags in git commits.

---
*Last updated:* `2026-05-27T08:40:13Z`
